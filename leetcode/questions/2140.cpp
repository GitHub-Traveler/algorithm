#include <climits>
#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
  long long mostPoints(vector<vector<int>> &questions) { long long result; }
};

int main() {
  Solution test = Solution();
  vector<vector<int>> questions;
  int sol = test.mostPoints(questions);
  cout << sol << endl;
  return 0;
}
