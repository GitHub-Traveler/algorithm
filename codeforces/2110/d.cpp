#include <bits/stdc++.h>
#include <vector>
using namespace std;
using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n = 0;
        int m = 0;
        cin >> n >> m;
        vector<i64> batteries(n, 0);
        vector<unordered_map<int, int>> path(m);
    }
}
